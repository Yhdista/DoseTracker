package com.yhdista.dosetracker.domain.model

/** Named part of the day a PERIOD-timed schedule resolves through the user's period times. */
enum class DayPeriod {
    MORNING,
    NOON,
    EVENING,
    NIGHT,
}
