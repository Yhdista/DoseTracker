<resources>

    <style name="Theme.DoseTracker" parent="Theme.MaterialComponents.Light" />

    <style name="Theme.DoseTracker.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/icon_bg_start</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DoseTracker</item>
    </style>
</resources>